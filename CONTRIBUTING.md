# 贡献指南

感谢你愿意为这个项目出力。

## 在开始之前

- **隐私红线**：本项目是诊所管理系统。提交 Issue、PR、测试数据、截图时，
  **绝不包含任何真实患者/员工信息**（姓名、电话、身份证、病历内容、数据库文件）。
  一律使用合成假数据。
- **架构约定**：新功能独立模块；依赖方向只允许 呈现 → 业务 → 数据；
  不做静默兜底（数据缺失就明确报错或显示「无数据」）；不留新旧格式双轨兼容层。
- 大改动请先开 Issue 讨论，避免方向性返工。

## 开发流程

1. Fork 并克隆仓库，`pip install -r requirements-dev.txt`。
2. 行为变更请先写失败测试，再最小实现让测试转绿（TDD）。
3. 提交前运行快速闸门：只跑本次改动直接影响的测试；
   前端 JS 改动同时跑 `node --check <文件>`。
4. 全量闸门（合并前）：`python3 -m unittest discover -s test -q` 与
   `node --test test/web/*.mjs` 必须全绿。
5. 小步提交：一个 PR 只交付一个可独立验证的结果，commit message 说清楚做了什么。

## 代码风格

- 后端：Python 3 + FastAPI + SQLite（标准库 sqlite3），无 ORM。
- 前端：原生 HTML/CSS/JS，**零构建**——不引入打包工具。
- 注释和提交信息使用中文或英文均可，说清楚「为什么」比复述「做了什么」更重要。

## 协议

贡献即表示你同意以本项目的 AGPL-3.0 协议授权你的修改
（版权人保留双授权权利，详见 README「协议与商业授权」一节）。
